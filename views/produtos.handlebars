<main>
    <div class="produtos">
        {{#each produtos}}
        <div class="card-produto">
            {{> card_produto}}
            <div class="btns">
                <button class="btn">Comprar</button>
                <a class="btn btn-detalhes" href="./detalhes?produto={{this.nome}}" >Detalhes</a>
            </div>
        </div>
        {{/each}}
    </div>
</main>